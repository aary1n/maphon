# SPEC — STO TE01δ Maser Cavity: Forward Model + Thermal-Robustness Pipeline

**Status:** authoritative source of truth for this repo. Derive CLAUDE.md / READMEs / module docs from this; do not contradict it. Where this spec and a paper's face value disagree, **this spec wins** (the disagreements are deliberate — see §6).

**Register:** standard physics / UQ terminology — *surrogate*, *Gaussian process*, *polynomial chaos*, *Sobol*, *parameter-free forward model*, *Bayesian re-inference*. The earlier "no machine-learning language in comments" convention is **retired**: it rested on a misread of the supervisor's stance (corrected directly — surrogate/ML framing is fine with him). Use the field's own terms; don't manufacture euphemisms for them.

---

## Revision note — this pass (thermal-droop pivot)

Prompted by the 2026-07-01 Oxborrow meeting. **The deliverable has moved** from static fabrication-tolerance yield [P(C > 1) across the build population] to **thermal-robustness of the maser's figures of merit** — the distribution, across as-built cavities, of how much operating heating each can absorb before cooperativity falls through the masing threshold.

Reason the old deliverable collapsed: Breeze's build runs C ~ 190, so "does an as-built cavity mase" is trivially yes regardless of fabrication spread. The interesting, unmodelled question is *operating margin*: pump-induced differential heating detunes the cavity from the spin line (STO εr is strongly T-dependent) faster than it detunes the two together, eroding C during operation. Wu's own single-pulse estimate for the STO ring is ΔT ~ 0.3 K; a back-of-envelope from Wu's coefficients puts the masing-killing differential heating at order ~0.5 K — i.e. the margin is *thin enough to be a real result*, not a foregone conclusion.

**What is unchanged:** §2 forward model, §3 extraction, §4 wall-loss decomposition, §5 validation gate, §8 analytic benchmark. The C₀ / κc engine is **repurposed, not invalidated** — it still produces C₀(geometry, εr, tanδ) and the loaded linewidth at the tuned operating point; only the post-processing on top changes, plus one new closed-form module.

**What is new:** an analytical thermal submodel (§7T, new `thermal/` module) and a retargeted output layer (§7, §10). Scope discipline: the shape-optimisation / cooling-channel / materials-survey / loop-gap thread that came out of the meeting is **out of scope** and lives only as a future-work pointer (§9).

**Provenance honesty (load-bearing — do not overclaim):** Oxborrow endorsed *thermal modelling of the resonator* as the direction and the *analytical heat-conduction method* (Bessel functions, cylindrical PDE + radiative/convective BCs, back-of-envelope). He has **not** explicitly ratified (a) the thermal-*budget-distribution* framing or (b) the Δf_max ∝ 1/√Q "higher-Q ⇒ less margin" hypothesis — both are ours, pending his written confirmation and a rigorous derivation (§7.T4, §11). Treat the plan below as current-best-pending-reply, not locked. *(Rung moved 2026-07-06 for (b) only: the question is now endorsed-to-pursue, verbal — see the dated block below and §7.T4; the result and (a) remain unratified.)*

**2026-07-02 update — Bayliss/Cowley-Semple resolution.** Oxborrow forwarded the Glasgow "linewidths" thread (2026-06-26, Glasgow/MIT/Imperial). The calibration-dataset identity/existence question (§11, item 5) is **resolved**; remaining asks are metadata, not existence. See §11 and §7.T5 for status and the resulting calibration-observable split.

**Attribution convention.** "Bayliss" = Prof. Sam Bayliss, University of Glasgow (PI, QOS group) — the *group* the phenomenon lives in, not the measurer. The measurements are **Angus Cowley-Semple's** (PGR, Bayliss group). Refer to the calibration data as the **Cowley-Semple ODMR dataset**; "Bayliss-calibrated" as shorthand is acceptable but the byline is Cowley-Semple's. Context: shared in the "linewidths" thread (2026-06-26) for the host–guest paper with Max Attwood (MIT) and Ziqiu Huang (Imperial); Sarah Mann and Oxborrow cc'd.

**Endorsement ladder — new rung (2026-07-02):** Oxborrow, independently on record (in-thread, 2026-06-26), applied Lang 2007's ~−0.05 MHz/K to Cowley-Semple's shifts and inferred pump heating of "several tens of Celsius." The *mechanism inference* (shift = heating) is therefore his, publicly, in the collaboration. The *calibration-target plan* (Phase 2 reproduces this dataset quantitatively, §7.T5) remains **ours-communicated, unratified** — do not conflate the two.

**2026-07-04 update — Oxborrow reply to the §11 note (email, received 2026-07-04).** Four things landed, each at its own rung:

1. **Endorsement ladder — new rung (2026-07-04): green field confirmed.** His sentence, on record: *"Neither Angus Cowley-Semple nor Ziqiu Huang is modelling thermal response."* The §11 item-10 "nobody-else check" is resolved: Phase 2's thermal-response modelling is an uncontested green field within the collaboration, and the novelty claim is now **Oxborrow-endorsed**, not just ours. (Corroborating detail from this pass's literature check: Mann 2025's author-contribution statement puts Ziqiu Huang on thin-film/nanocrystal preparation and characterisation — no modelling of any kind visible.) What this does **not** ratify, unchanged: the budget-distribution framing, the Q-margin hypothesis (§7.T4), and the calibration-target plan (§7.T5) — all still ours-communicated, pending.
2. **Calibration-rig geometry — OXBORROW-UNCONFIRMED (his words: "my understanding" / "could be"):** Angus's sample is a thin flake of Pc:PTP stuck to a substrate (microscope slide) for handling, interrogated optically through a microscope objective, spot size possibly microns. Treated as **planning assumptions pending Angus ratification**; checked against the published record this pass — mounting corroborated, thinness and micron spot **not** corroborated (details and consequences in §7.T5 rig-geometry note; asks in §11 item 5).
3. **Mitigation brainstorm — UNRATIFIED:** sapphire multiblade-waveguide heat sink; fluid capillaries in the maser crystal. Logged at §7.T6 / §9; off the critical path.
4. **Process:** Oxborrow makes the Angus introduction **Monday 2026-07-06 — he asked to be reminded that morning**; most §11 item-5 asks now route through Angus directly.

Same pass: Singh et al., Nat. Commun. 16, 10530 (2025) — §11 item 7's must-pull — obtained and graded into §6T. X–Z region-III slope **−101 kHz/K** (the "RT-valid" reading was downgraded by the later SI pass, below); the paper's headline 247 kHz/K is the T_xy transition *at the 193 K phase transition* and must never be used as the RT coefficient.

**2026-07-04 update (second pass, same day) — both SIs in hand; Singh Fig. 2B(iii) reanalysed.** The Mann JACS SI (ja5c05505_si_001) and Singh SI (41467_2025_65508_MOESM1_ESM) are now attached in Zotero and were read end-to-end. Outcomes, at their evidence rungs:

1. **Mann SI — the cw-path metadata the §7.T5 asks wanted is *not there*:** no spot size, no beam diameter/M², no absolute laser power at sample (only "lower laser power" for DAP, unquantified), no deuterated samples anywhere in main or SI. What it *does* settle: sample geometry re-corroborated (0.01% crystals = 0.5-mm polished plates; the only published 0.1% Pc:PTP is still a 100-nm OMBD film on glass — with the motive stated outright: "for maximal contrast, we use thin films"); pulsed duty cycles printed (5 µs init + 1 µs readout per 600 µs for Pc); cw-ODMR is genuinely cw. **New wrinkle: the published record now holds *two* Glasgow rig configurations** — Mena 2024 (520 nm LP520-SF15A diode; f = 30 mm doublet on the cw path; CPW) vs Mann 2025 (532 nm Nd:YAG "Finesse"; one f = 11 mm, NA 0.24 aspheric C061TMD-A focusing *both* cw and pulsed; 1-mm loop antenna for crystals, CPW for films). Which rig took the Cowley-Semple dataset is now itself an Angus ask; the dataset's drive-current axis (50–100 mA) smells like the diode rig, unconfirmed. Details folded into §7.T5; asks sharpened in §11 item 5.
2. **Singh SI — the hoped-for error bar is definitively absent:** SI Table S1 prints the slopes bare too; raw data exist as a Zenodo record (doi 10.5281/zenodo.17231876) with **files restricted**. In its place, a **vector reanalysis of Fig. 2B(iii)** (the figure is pure vector; 197 marker centres, tick-exact calibration — archived in `refs/singh_fig2biii_reanalysis.md` + `.csv`): the paper's own drawn region-III fit line has slope **−112 kHz/K** over cold-finger 254–324 K (OLS on the extracted points reproduces it exactly); the printed −101 matches **no** stated window; OLS over the SI's region III (cold-finger 150–330 K) gives −68.4 ± 1.7; curvature is real and monotonic (local slope ~0 at cold-finger 180–210 K → ~−130 at 290–320 K). The x-axis is the **cold-finger**: the "abs. T 193 K" marker is drawn at cold-finger ≈ 138.5 K ⇒ laser-heating offset ≈ **+55 K at 110 mW cw** — so the −101/−112 window samples actual ~310–380 K, above RT. Offset-corrected, Singh's own RT-local slope reads ≈ −70…−80 kHz/K, landing on Lang/W20. **Grading consequence (§6T):** −101 stays as the conservative band edge only; the −50…−101 band is now empirically supported rather than merely prudent. A deuteration-transfer caveat (protonated Singh crystal vs the dataset's Pc-d₁₄:PTP-d₁₄) is added alongside in §6T and `provenance/constants.py`.

**2026-07-06 update — supervisor meeting (in person; contemporaneous notes). Every item below is Oxborrow-VERBAL — grade at that rung, not as literature.** Five outcomes:

1. **Doping below nominal — CONFIRMED, with a direction (new §6T caveat grade: Oxborrow-verbal 2026-07-06).** Bridgman growth does NOT incorporate pentacene at the nominal doping: the growth acts as a zone-refining step, so the crystal holds LESS pentacene than the melt (it also purifies the p-terphenyl host). Direction known, factor unknown: nominal overstates [Pc] ⇒ any absorption length l_abs computed from nominal doping overstates absorption and UNDERSTATES penetration depth. Consequences folded through: the volumetric appendix's "prefer measured penetration over nominal-doping arithmetic" (§7.T5 rider) is upgraded from prudent to supervisor-backed; check 3b's concentration ratio cannot use nominal values without this caveat (§7.T5); the nominal-vs-actual flag previously carried at UNSOURCED-SCOPING is now real and directional (§6T; `PumpAbsorptionLength` and `Crystal` in `provenance/constants.py`; c_dope row of the provenance table).
2. **Q-margin hypothesis — endorsement ladder rung moved (2026-07-06): question ENDORSED to pursue; RESULT still unratified.** Oxborrow thinks the 1/√Q "higher-Q ⇒ less thermal margin" idea is worth deriving. §7.T4 moves from "ours, unratified" to "supervisor-endorsed as worth pursuing"; the result awaits the rigorous derivation (§11 item 9) — he blessed the question, not a number. Keep the two distinct. (The budget-*distribution* framing is untouched by this and remains unratified.)
3. **Angus ask list — FINALISED at three** (§11 item 5): (i) per-sample form + thickness for the deuterated (Pc-d₁₄) dataset samples, both concentrations; (ii) which rig + cw spot size; (iii) laser power at the sample. Everything else folds in behind as riders; the rationale lines from the 2026-07-04/05 passes still hold. Same pass: the **published Mena PRL SI is now in hand** (0.5 mm plate quote verbatim; cw path = f = 30 mm doublet AC254-030-AB; the high-NA aspheric is the PULSED path) — the §7.T5 reference is upgraded from "Methods as printed in arXiv:2402.07572" to the published SI. (Zotero attachment key not verified this pass — desktop API unavailable; attach alongside the Mann/Singh SIs.)
4. **NEW modelling output — inhomogeneous thermal line broadening, reported in LINEWIDTHS (the observable Oxborrow explicitly asked for).** The thermal model must predict not just the mean spin-frequency shift but the LINE-SHAPE consequence of the non-uniform ΔT field: the ensemble line is shifted by the probe-weighted mean and broadened by the probe-weighted spread, both expressed in units of the resonance linewidth (equivalently Δf·Q/f = "how many linewidths"); absolute Hz is secondary. Spec'd as §7.T2 output 3; implemented in `cavity.thermal.broadening` on the existing ΔT-field machinery, with the uniform-T ⇒ zero-width limit anchor in CI. Framing consequence, recorded at §7.T4: this is the SAME physics as the Q-margin question — margin is linewidths-of-detuning before falling off resonance, and the inhomogeneous width is that displacement made observable. It is also the concrete thing the spatially-resolved model delivers that Wu's single lumped ΔT cannot (§7.T3).
5. **Radiation-balance sensitivity flag (new §7.T7; a check, not a rebuild).** In poor thermal conductors — p-terphenyl is the project premise — thermal RADIATION becomes significant relative to conduction "sooner" than good-conductor intuition suggests: quantify where the radiative boundary term stops being negligible vs the conductive term across the plausible operating range, rather than assuming it small. Riders recorded in the same place: (a) a more collimated beam drives light deeper and avoids an interface hot spot — independent (verbal) support for the volumetric Beer-Lambert deposition already appended, not surface-only; (b) the sapphire "dagger" heatsink is itself not a free cold boundary (the sapphire has to be cooled too) — §7.T6's BC variant is reframed as a finite-conductance contact, not an idealised fixed-T surface. No heatsink modelling now.

---

## 0. Purpose & scope

Build a validated COMSOL forward model of the strontium-titanate (STO) TE01δ dielectric-ring maser resonator (**Phase 1**), then a surrogate-accelerated Monte Carlo layer that propagates fabrication + material scatter into the **thermal robustness of the maser's figures of merit** (**Phase 2**, the owned deliverable). 8-week UROP; **results are the priority, manuscript is post-placement.**

Phase 1 reproduces published numbers. Phase 2 produces the thing the literature lineage left undone: not point values of Q / V_mode / F_m / C, and not a trivial static yield number, but the **distribution of thermal operating margin** — per as-built cavity, how much pump-induced heating (ΔT, equivalently cavity detuning Δf) it can absorb before C drops through 1 — together with the tolerance/design levers that set that margin, each with quantified error.

---

## 1. Architecture / stack

- **COMSOL Multiphysics, RF module**, driven via **LiveLink for Java / MPh (Python wrapper over COMSOL's Java API)** — validated available. Python owns sweeps, surrogate, Monte Carlo, thermal submodel, analysis; COMSOL owns the electromagnetic FEM eigensolve only.
- **The thermal layer is analytical, not FEM.** No Heat Transfer Module, no COMSOL solve for thermal. It is a closed-form / semi-analytical heat-conduction submodel (§7T) evaluated in Python. This keeps it licence-free and CI-testable, and is exactly the "back-of-envelope" scope the supervisor framed.
- **Transplant the SiPhON Monte Carlo yield infrastructure** (Latin-hypercube/Sobol sampling, surrogate fit, aggregation, sensitivity indices) as the I/O + aggregation skeleton. Port, do not rewrite. See SPEC §7 (expanded) for the precise transfer boundary.
- COMSOL is **not** assumed available in CI. Solves run locally / on cluster. CI tests the Python layer (extraction maths, surrogate, MC, analytic benchmark, thermal submodel) against cached/synthetic field data.
- **Reproducibility:** pin RNG seeds; log COMSOL version, mesh settings, and element count with every solve; persist raw complex eigen-solutions (not just scalars) so extraction and thermal re-derivation can be re-run without re-solving.

---

## 2. Physics model (Phase 1) — UNCHANGED

- **2D axisymmetric (r, z), azimuthal index m = 0.** `Electromagnetic Waves, Frequency Domain` interface, **Eigenfrequency** study, search near 1.45 GHz.
- **Domains:** rectangular copper box in the r–z half-plane → **Impedance Boundary Condition** on walls (and a **PEC variant**, see §4). Air fill (εr = μr = 1, σ = 0). One dielectric region (STO).
- **STO material:** real permittivity εr′ = **316.3**; loss entered as **complex permittivity** εr = εr′(1 − i·tanδ), with **tanδ = 1.1×10⁻⁴** at 1.45 GHz. μr = 1, σ = 0. (Provenance: §6.)
- **Copper:** σ = **6.0×10⁷ S/m**, μr = 1, via Impedance BC (surface resistance R_s = √(ωμ₀ / 2σ)).
- **`dielectric_shape` ∈ {puck, torus} — switchable geometry parameter.** Booth's appendix under-specifies the cross-section (gap #1, §11); implement both and let §4 decide which reproduces the targets.
- **Nominal geometry** (Booth App. A, STO TE01δ): box width **12.28 mm** (→ box radius 6.14 mm), box height **18.42 mm**, dielectric radius **2.46 mm**. The **dielectric height (puck) / minor radius (torus) is UNPINNED** — expose as a parameter, sweep it, and/or await Booth's `.mph`.
- **Mesh:** extremely fine, **fully curved dielectric boundary**. Run a convergence study: refine until f and Q are stable to the target significant figures; record the converged element count.
- **Mode identification:** identify TE01δ by **field pattern**, not eigenvalue order. Implement an automatic field-symmetry check; reject and re-pick if the symmetry test fails.

---

## 3. Extraction — the careful core (axisymmetric Jacobian made explicit) — UNCHANGED

Every volume integral in 2D axisymmetric form carries the **2πr Jacobian**: ∭ g dV = 2π ∬ g · r dr dz.

- **f** = Re(eigenfrequency).
- **Q** — COMSOL returns complex f = f′ + i·f″ and exposes `emw.Qfactor`. Booth: Q = ω/(2|δ|), λ = δ − iω, i.e. **Q = f′ / (2 f″)** with sign care. **Assert against §8** (PEC + lossy-dielectric limit ⇒ Q = 1/(p_e·tanδ)).
- **V_mode** (magnetic) = 2π∬ |H|²·r dr dz / max(|H|²). **Report two variants**: max taken (a) globally, (b) locally over the dielectric/gain region.
- **p_e** (electric energy filling factor) = ∭_dielectric ε|E|² dV / ∭_all ε|E|² dV. Required to interpret Q.
- **F_m** (magnetic Purcell factor) — **F_m = (3 / 4π²) · λ³ · (Q / V_mode)**, λ = c/f. Validate once against Breeze Table 1 (STO: Q=10⁴, V=0.2 cm³, f=1.45 GHz → F_m ≈ 3.4×10⁷ vs tabulated 3.6×10⁷).

**Now doubly load-bearing for Phase 2:** the **loaded** cavity linewidth κc = ωc/Q_L and the **gain-region H** (field the spins actually see) feed the thermal budget directly (§7T, §7.T2). Extraction must expose both cleanly, with the loaded/unloaded Q split single-sourced (§7.3 convention guard).

---

## 4. Wall-loss decomposition (closes the geometry gap empirically) — UNCHANGED

Run the nominal geometry **twice**: (a) Impedance-BC walls → **Q_total**; (b) PEC walls → **Q_diel**. Then **1/Q_wall = 1/Q_total − 1/Q_diel**.

**Acceptance:** at tanδ = 1.1×10⁻⁴, expect **Q_diel ≈ 9,000–10,000**, **wall-loss fraction ~23–27%**, **Q_total ≈ 6,980** (Booth Table 8). If the assumed cross-section reproduces this, gap #1 is closed from the physics side. This is the proof that the model's confinement physics is correct *before* Phase 2 leans on it. (The 30% Breeze/Booth Q gap is confinement, not loss tangent — full derivation in §6.)

---

## 5. Validation gate (Phase 1 complete = all pass) — UNCHANGED as the gate

| Check | Target | Source |
|---|---|---|
| Analytic benchmark passes | empty-cavity TE011 < 0.1% error | §8, build FIRST |
| f | 1.45 GHz, ≥4 s.f. | Booth, Breeze |
| Booth two-point | Q ≈ **6,980**, V_mode ≈ **0.409 cm³** at Booth geometry (walls on) | Booth Table 8 + App. A |
| Confinement trend | tightening toward V_mode ≈ 0.2 raises Q toward **~10,000** | Breeze Table 1 |
| Wall-loss split | Q_diel ≈ 9–10k, wall fraction ~23–27% | §4 |
| F_m | order 10⁷ via §3 formula | Breeze (STO F_m = 3.6×10⁷) |

**The gap must reproduce as a continuous confinement trend, not two unrelated points.** §5 still gates **first, unconditionally** — no Phase 2 (static or thermal) work until it passes.

### 5a. Pre-Phase-2 checkpoint — run *your* model for Booth's real C₀ and κc (NEW, required before §7T is trusted)

Everything about "C ~ 190, ~0.5 K kills it" is currently Breeze's numbers + Wu's coefficients — **not** your forward model on Booth's actual geometry, which carries a real (if modest) Q gap. Once §5 passes, the first real thermal checkpoint is: run the validated model to extract Booth's **own** C₀ and loaded κc at the tuned operating point, and confirm the "decisively above threshold, thin thermal margin" story holds with your numbers before building the thermal layer on it. If C₀ or κc land materially off the assumed values, the margin arithmetic — and possibly the framing — changes. Cost: hours, once §5 is green.

### 5b. Phase 1b — bore + crystal (required for §7 and §7T, not for the gate) — ELEVATED

Add the central **bore** (hollow ring) and the **pentacene:p-terphenyl crystal** sub-domain (3 mm × 8 mm, 0.053% doping; Breeze 2017). Crystal εr < 5, μr ≈ 1 — Booth argues it barely perturbs the mode; **verify, don't assume.** This is now more critical than under the static plan, because it delivers **two Phase-2 inputs**: (i) the gain-region H the thermal coupling needs, and (ii) the plate-sensitivity ∂C/∂p_tune that decides whether §7.3 is coupled or decoupled. Both feed §7T.

---

## 6. Parameter provenance (authoritative — use these, not paper face values)

Live source-of-truth is `pentacene_maser_parameter_provenance.md` (graded). Electromagnetic findings **unchanged**:

- **εr:** standardise on **316.3–318**. The 1.9% spread shifts f by ~14 MHz (tuning-range matter, not yield pass/fail).
- **tanδ:** nominal **1.1×10⁻⁴** at 1.45 GHz (Booth's Debye-scaled 22-GHz value; optimistic end). Measured-device effective loss spans **1.0–2.3×10⁻⁴** (Breeze Q₀ ≈ 10,700 ↔ Wu Q₀ ≈ 4,320; ~2.5× spread).
- **Modelled Q = tanδ × confinement.** Breeze 10,000 ≈ dielectric ceiling (walls negligible, V=0.2); Booth 6,980 = same tanδ + ~25% wall loss (V=0.409). The 30% gap is confinement, not loss tangent.
- **Cu:** σ = 6.0×10⁷ S/m.
- **Crystal:** pentacene:p-terphenyl, 3 mm × 8 mm, 0.053%; εr < 5 (Breeze 2017).
- **De-loading convention:** k = 0.2 (Breeze 2017), Q₀ = Q_L(1 + k). Wu's coupling unstated — flagged (§11).

### 6T. Thermal provenance (NEW block — to source; feeds §7T)

The thermal submodel needs material and coupling constants that are **not yet in the provenance file**. Pull, grade, and single-source them exactly as the EM parameters:

- **c_p — primary in hand: Chang 1983, J. Chem. Phys. 79, 6229** (p-terphenyl adiabatic calorimetry; cited as Wu ref [45]). C_p = **0.94·T J K⁻¹ mol⁻¹ (200–480 K)** ⇒ c_p(300 K) ≈ **1225 J kg⁻¹ K⁻¹** (M = 230.29 g mol⁻¹). λ-transition peak **193.55 K** = hard low-T validity bound for the linear form (operating point is safely above; state it once). Note c_p is **not constrained by the calibration dataset** (§7.T5) — it enters transients only; Chang carries it alone. ρ still needs sourcing.
- **k — provenance floor found: Hedley, Milnes & Yanko 1970 (JCED 15, 122).** ***Liquid*** p-terphenyl k ≈ **0.134–0.136 W m⁻¹ K⁻¹ at 213–254 °C**. The unreferenced ~0.1 W m⁻¹ K⁻¹ crystal figure (Breeze-2018 lineage) coincides suspiciously with the liquid value — plausible provenance origin. Crystals conduct better than their melts ⇒ treat **0.1 as a floor**, keep the 0.1–1 band and ~2× anisotropy. **k is the parameter the CW calibration actually constrains** (steady-state ΔT ∝ 1/k for conduction-dominated transport) — the open literature gap and the new data point at the same quantity. (Stretch framing, do not headline unlabelled: with laser power + spot size calibrated, the fit is an effective-k constraint on doped p-terphenyl via triplet thermometry.)
- **STO frequency–temperature coefficient df/dT** at the operating point. This is the *mechanism* of thermal droop: STO is an incipient ferroelectric / quantum paraelectric with strongly T-dependent εr, and f ∝ εr^(−1/2), so small ΔT → large Δεr → large Δf. Source the low-field εr(T) slope (Goryachev / Rupprecht-adjacent literature already in the library) and convert to df/dT.
- **df_spin/dT — pinned, with a spread to carry:** negative. **Lang, Sloop & Lin 2007 (JPCA 111, 4731) Fig. 4 — in refs** (provenance doc "must-obtain" #5): X–Z transition, ~**−70 kHz/K** average over 230–296 K, **nonlinear** (steepens toward the 193 K transition) → fit *locally* around operating T, do not adopt a single global number. Spread across sources: Oxborrow in-thread quotes **−50 kHz/K** (RT); **W20 prints −80 kHz/K citing the same Fig. 4** — a ±40% spread on one curve depending on where it is read. Carry as the coefficient prior in §7.6 channel 3 (SPEC §7-expanded). Second primary — **pulled and verified 2026-07-04: Singh et al. (Ajoy group, Berkeley — NOT Bayliss), Nat. Commun. 16, 10530 (2025), doi 10.1038/s41467-025-65508-2.** 0.1% Bridgman-grown Pc:PTP single crystal, CW-ODMR vs temperature: **X–Z (1.45 GHz) slope −101 kHz/K** from the region-III linear fit (monoclinic phase; region III = **cold-finger 150–330 K** per SI Table S1 footnote — "includes RT" only in cold-finger readings; Fig. 2B(iii) red line; sign from blue-shift-on-cooling, Fig. 1D/E caption). **Do not use the paper's headline 247 kHz/K:** that is the T_xy (107 MHz) transition *inside the 193 K phase-transition region* (region II; Table 1 footnote "taken from phase transition region") — not an RT coefficient, not the maser transition. Caveats — upgraded by the 2026-07-04 SI pass + Fig. 2B(iii) vector reanalysis (`refs/singh_fig2biii_reanalysis.md`): **no uncertainty exists in print** (main text and SI Table S1 both bare; Zenodo raw-data record 10.5281/zenodo.17231876 has restricted files — request access if the ± becomes load-bearing). The reanalysis (197 extracted points, tick-exact axes) shows statistical error is negligible (±1–2 kHz/K) and the real uncertainty is the **fit-window systematic**: −68.4 (cold-finger 150–330 K, the SI's region III) → −88 (200–330) → −97 (220–330) → **−112 over 254–324 K, the span the paper's own red line is actually drawn on** (OLS reproduces the drawn line exactly; the printed −101 matches no stated window and disagrees with the paper's own fit line). The temperature axis is the **cold-finger**: the 193 K marker sits at cold-finger ≈ 138.5 K ⇒ laser-heating offset ≈ **+55 K at 110 mW cw**, so the −101/−112 window samples actual ~310–380 K, above RT; offset-corrected, Singh's RT-local slope reads ≈ −70…−80 kHz/K — consistent with Lang/W20. Curvature confirmed, monotonic (~0 at cold-finger 180–210 K → −130 at 290–320 K); the flat stretch maps to actual ~225–265 K where Lang shows ≈ −50, so either the offset is *not* constant (contradicting the authors' assertion) or the samples differ — both readings forbid importing −101 as a local RT coefficient. Verdict unchanged in direction, now empirical: treat **−101 as the conservative band edge, not a replacement local fit**. **Deuteration caveat (carry alongside):** Singh's crystal is protonated Pc:PTP; the calibration-dataset samples are Pc-d₁₄:PTP-d₁₄ — df_spin/dT transfer is assumed small (host-lattice-expansion mechanism, nominally same lattice) but unverified by any measurement in hand. Single-sourced in `provenance/constants.py` (§6T block). Magnitude check vs the cavity arm stands: |df_spin/dT| ~ 50–101 kHz/K ≪ df_cavity/dT ~ +2.6 MHz/K; **note opposite signs — the differential adds.**
- **Cowley-Semple ODMR dataset (calibration target; status + asks in §11, item 5; observable-split and checks in §7.T5).** Observable: **spin** resonance vs laser current, steady-state CW, Glasgow rig (no STO, no cavity). Source-term corollary now empirically supported: 0.1% shifts ≫ 0.01% at identical power ⇒ **q̇ ∝ [Pc]·I_pump** (absorption in the pentacene, not host background).
- **Pump illumination model** — incident optical intensity / absorbed power density in the crystal. **Wu's numbers are from a different pump setup**; do not inherit them silently (see §11). Parameterise incident intensity; treat absorbed-power → heat as the source term. **Doping-vs-absorption caveat (Oxborrow-verbal, 2026-07-06):** Bridgman growth zone-refines — actual [Pc] sits BELOW nominal by an unknown factor (and the host is purified in the same pass), so any absorption arithmetic from nominal doping is biased in a KNOWN direction: it overstates absorption and understates the penetration depth l_abs. Prefer measured penetration data (Takeda 2002 / Breeze-era, still to pull) over nominal-doping arithmetic — now supervisor-backed, not merely prudent (§7.T5 rider; `PumpAbsorptionLength` / `Crystal` in `provenance/constants.py`).

---

## 7. Phase 2 — thermal-robustness pipeline (the deliverable)

The detailed statistics live in **SPEC §7 (expanded)** — surrogate design, CV gate, error budget, Sobol, tolerance curves. This section fixes the *architecture* of the pivot; the expanded doc is being revised in lockstep (its static-yield output framing is superseded here — the machinery survives, the output changes).

**Three-layer structure:**

**Layer A — static forward UQ (was the deliverable; now the intermediate).** Validated nominal model → parametric COMSOL sweep over fabrication/material DOFs → **validated, uncertainty-bearing surrogate** (PCE primary, GP cross-check/active-learning) per output → LHS/Sobol sample the surrogate (10⁴–10⁵ draws) → **distributions of C₀ and loaded κc at the tuned operating point** across the build population. This is the SPEC §7-expanded content, intact. COMSOL is called only to train the surrogate (O(150–300) solves total), never per MC draw. The old static yield P(C₀ > 1) is **dropped as a headline** (trivially ~1 at C ~ 190) but its producing machinery is exactly what Layer C consumes.

**Layer B — analytical thermal submodel (NEW, §7T).** Closed-form heat conduction on the *existing* geometry → the map from pump illumination to differential detuning: ΔT(r, z) field, and via §6T's df/dT the coefficient **Δf(P_pump)** (equivalently df/dΔT). No COMSOL. Explicitly **not** a shape or materials search (§9).

**Layer C — thermal-budget distribution (the owned result).** Per sampled build θ, at its tuned operating point:
1. Max tolerable cavity–spin detuning before C falls through threshold:
   **Δf_max(θ) = (κc/2)·√(C₀ − 1)**  — cavity-linewidth-limited, from a Lorentzian cooperativity roll-off C(Δ) = C₀ / (1 + (2Δ/κc)²).
2. Convert to a **thermal budget** via Layer B: ΔT_max(θ) = Δf_max(θ) / |df_cavity/dT − df_spin/dT|, and further to a **pump-power budget** P_max(θ) via the ΔT(P_pump) coefficient.
3. **Report the distributions** p(Δf_max), p(ΔT_max), p(P_max) across the build population — with error bars (sampling + emulator, per SPEC §7-expanded §7.6). *This is the literature's missing quantity: no one has error bars on the maser's thermal operating margin.*

**DOFs + input distributions:** as SPEC §7-expanded §7.2/§7.4 — εr, tanδ, dielectric radius/height, box dims, bore radius + eccentricity; tuning plate is a **control** (solved per device to hit f_spin), not sampled. Thermal coefficients (§6T) enter Layer B, not as sampled noise unless their scatter is itself of interest (stretch).

**Sensitivity → design lever (SPEC §7-expanded §7.7):** Sobol on p(ΔT_max) — which DOFs set the margin. Plus the Q-vs-margin analysis below, which is the scientific hook.

### 7.T4. The Q-vs-margin hypothesis (question ENDORSED to pursue — Oxborrow-verbal 2026-07-06; RESULT still ours, unratified)

Napkin result: κc = ωc/Q_L ⇒ κc ∝ 1/Q; and if C₀ ∝ Q with N, g_s, κ_s fixed, then Δf_max = (κc/2)√(C₀ − 1) ∝ (1/Q)·√Q = **1/√Q**. I.e. **higher-Q builds have *less* thermal frequency margin, not more** — linewidth narrows faster than the static cooperativity gain. If it survives scrutiny this is the paper's actual scientific content (not the outcome-known-in-advance static yield).

**Endorsement rung (2026-07-06, verbal):** Oxborrow thinks this is worth deriving — the *question* is supervisor-endorsed; the *result* is not ratified and awaits the derivation below (§11 item 9). He blessed the question, not a number; do not conflate.

**Linkage (2026-07-06) — one quantity, two views:** the §7.T2 output-3 inhomogeneous line observable and this hypothesis are the same physics. "Margin" is how many linewidths of detuning fit before the system falls off resonance; the inhomogeneous width is that displacement made observable. The derivation and the observable share machinery and the linewidth reporting unit (Δf·Q/f; `cavity.thermal.broadening`).

**Two hazards before this is a headline, not a napkin:**
- The clean scaling assumes C ∝ Q with everything else fixed. **N, g_s, κ_s may carry their own geometry/Q dependence** (via V_mode, gain-region H, confinement) that breaks it. Derive C₀ and κc's *joint* dependence on the geometry DOFs from Layer A rather than asserting C ∝ Q.
- The map to ΔT_max (not just Δf_max) folds in df/dT, which may itself vary across the build population. Check whether the 1/√Q trend holds in ΔT space, not only in Δf space.

Test empirically against the Layer A/C samples (does p(ΔT_max) anti-correlate with sampled Q?) *and* derive it, before claiming it.

---

## 7T. Analytical thermal submodel (NEW module: `thermal/`)

Closed-form / semi-analytical, Python-only, CI-testable. This is the §6T-fed engine of Layer B. Scope = the supervisor's "how hot does a piece of pentacene get, back of the envelope," on the **existing hardware** — no geometry *design* departure. One transport core, **two evaluation geometries**: the Glasgow calibration rig (flake/plate on a substrate, §7.T5 rig-geometry note) and the maser crystal in the STO ring (the Phase-2 target). "No departure" forbids inventing new resonator shapes; it does not license modelling the rig as a geometry it isn't.

**7.T1. Physical model.** Steady-state (and, if pulsed-pump timescales matter vs. thermal diffusion time, transient) heat conduction in the p-terphenyl host with an optical-absorption source term:
- Governing PDE: ∇·(k∇T) + q̇(r, z) = ρc_p ∂T/∂t, cylindrical (r, z), axisymmetric — the same symmetry as the EM model.
- Source q̇: absorbed pump power density from the illumination model (§6T).
- **Boundary conditions:** radiative and convective at free surfaces; conductive to the substrate held near room temperature (Oxborrow's "substrate below at room temperature" framing). Bessel-function radial solution in cylindrical coordinates (his explicit steer).
- **Geometry split (2026-07-04):** the calibration rig is not a cylinder — it is a thin plate/flake on a layered substrate under a focused (≈Gaussian) spot. That problem is *still* axisymmetric (about the **beam** axis, not the cavity axis) and still Bessel-family: the rig's analytic anchor is **Gaussian-spot spreading resistance on a layered medium (Hankel transform)**. The cylinder Bessel/Robin anchor is not displaced — it remains the anchor for the **maser-crystal geometry** (§7.T5 observable (b), the Phase-2 target).
- **Thickness matters** (his flag): do **not** default to a semi-infinite half-space; the finite crystal thickness changes ΔT. Start with a first-approximation (semi-infinite or a simple finite slab) *stated as such*, then check thickness sensitivity. (For the rig, thickness enters through the w/t regime — §7.T5.)

**7.T2. Outputs.**
1. **Δf(P_pump), our geometry (the pipeline output).** The scalar(s) Layer C needs: the effective ΔT the mode/spins experience for a given pump, weighted by the **gain-region H** (§5b) so it reflects the field the spins actually see, converted via §6T's df/dT to Δf(P_pump). Report both the spatial ΔT field (for sanity / the writeup figure) and the reduced coefficient (for the pipeline).
2. **Predicted steady-state ν_spin(I_laser), Cowley-Semple's geometry (the validation observable, §7.T5).** Under the Glasgow rig's conditions (no STO, no cavity), both concentrations. This is *not* the pipeline output — it is what the submodel is checked against before output 1 is trusted. (Predicted Δf_cavity(t) for our device remains model-only: it is an output of transferring the validated transport core to our geometry, not of calibration itself — see §7.T5.)
3. **Inhomogeneous thermal line observable, in LINEWIDTHS (NEW 2026-07-06 — the reporting unit Oxborrow explicitly asked for, verbal).** The gain/probed volume is not isothermal: ΔT(r,z) maps, through df_spin/dT (§6T), to a distribution of local spin frequencies, so the ensemble line is both SHIFTED (by the probe-weighted mean) and BROADENED (by the probe-weighted spread), on top of the intrinsic linewidth. Derive from the existing ΔT field, analytically/semi-analytically: (a) mean thermal shift ⟨Δf⟩ = (df_spin/dT)·⟨ΔT⟩; (b) inhomogeneous width = |df_spin/dT|·σ_T (probe-weighted RMS — an RMS width, not a FWHM; no convolution model claimed); (c) BOTH divided by the resonance linewidth — "how many linewidths", equivalently Δf·Q/f, is the headline unit; absolute Hz secondary. Implemented in `cavity.thermal.broadening`: a geometry-agnostic map (any weighted ΔT sample set → the observable pair — the future maser-cylinder or depth-resolved fields plug in unchanged) plus the Gaussian-spot rig instance on the §7.T5 surface field (probe weight = pump intensity, excitation/collection co-located; RADIAL spread only — depth weighting is deferred until a depth-resolved field evaluation exists, stated in the module as its accuracy boundary). CI anchors (§8 discipline): **uniform T ⇒ zero inhomogeneous width, pure shift — exact**; half-space elliptic-integral and 1-D-limit closed-form moments; a Hankel-Parseval identity (probe-weighted mean = centre rise of a √2-wider spot) as independent algebra. Linkage: same quantity as §7.T4's margin — recorded there. Upgrade statement (§7.T3, without re-claiming): a lumped single-ΔT model predicts a shift with identically zero width; the line-shape consequence is exactly what spatial resolution adds.

**7.T3. Standing on Wu, sharpened.** This replaces Wu's crude bulk-heat-capacity ΔT estimate (single lumped number) with a spatially-resolved, BC-honest calculation on the actual geometry — genuinely unprecedented per Oxborrow, and the concrete upgrade that earns the "nobody's done this" credit without touching resonator shape.

**7.T4. Convection question (bounded, not a research program).** Oxborrow asked "how effective is convection for dissipation." Answer it as a **coefficient sensitivity** — vary the convective BC over a plausible range, report how much ΔT_max moves — **not** as a cooling-geometry design study (ducts / channels / high-surface-area shapes are §9 future work).

**7.T5. Calibration against the Cowley-Semple ODMR dataset.** The illumination-induced frequency-shift data raised as "Bayliss's" is identified: the **Cowley-Semple ODMR dataset** (Angus Cowley-Semple, Bayliss group, Glasgow; status + remaining asks in §11, item 5). This is the "measure, don't assume" anchor for the submodel — but it constrains a narrower slice of it than the device prediction, and the two must not be conflated.

- **Two observables; do not conflate.**
  **(a) Calibration observable — spin, Glasgow geometry.** The Cowley-Semple measurement is the **spin transition** via ODMR in a rig containing **no STO and no cavity**. Chain: pump → crystal ΔT(r,z) → ν_spin shift via df_spin/dT (§6T), ΔT weighted over the optically probed volume (≈ the illuminated spot; excitation/collection co-located). **No cavity path, no STO, no mode weighting enters the calibration.** This isolates exactly the "how hot does the layer get" question, with the triplet as in-situ thermometer.
  **(b) Prediction observable — cavity-dominated, our geometry.** In the maser, the *device-relevant* frequency response is the differential detuning: cavity arm (+2.6 MHz/K via STO εr(T), E-energy-weighted over the STO) vs spin arm (−50…−80 kHz/K, gain-region-H-weighted). The cavity arm dominates the *magnitude* — and remains **model-only**: nothing in the Glasgow thread observes it. State this as the asymmetry it is: spin arm calibrated, cavity arm predicted.
- **Rig geometry (recorded 2026-07-04 — planning assumption, pending Angus ratification).** Oxborrow's description, explicitly his-understanding-unconfirmed: thin Pc:PTP flake stuck to a microscope slide for handling, interrogated through a microscope objective, spot possibly microns — i.e. **not a bulk cylinder**. Checked against the published record this pass (Mena et al., PRL 133, 120801 (2024) — Methods as printed in arXiv:2402.07572, **and, since 2026-07-06, the published PRL SI itself, in hand**: it carries the 0.5 mm plate quote verbatim, puts the f = 30 mm doublet AC254-030-AB on the **cw** path, and confirms the high-NA aspheric as the PULSED path only — cite the published SI, not the arXiv Methods, from here on (Zotero attachment pending, alongside the Mann/Singh SIs); Mann et al., JACS 147, 22911 (2025) = arXiv:2503.24341):
  - **Literature-supported (promote; pending Angus confirmation for the specific dataset):** crystals are mounted on a **glass slide with paraffin wax** (Mena Methods: "attached to a glass slide using paraffin wax before being polished into plates"); MW delivered via **PCB coplanar waveguide** (short-terminated for pulsed work); excitation is a **520 nm laser diode** (Thorlabs LP520-SF15A); the cw-ODMR focusing optic is the **f = 30 mm achromatic doublet** (AC254-030-AB) already noted in §11 item 5; PL detected >600 nm.
  - **NOT corroborated (stay assumptions):** *"thin flake"* — the published crystals are **0.5-mm-thick polished plates** (thickness set with 0.5 mm silicon guide plates); *"micron spot" / "microscope objective"* — no objective or spot size appears in the **cw** path (the high-NA aspheric is the *pulsed* path only; cw spot under an f = 30 mm doublet is plausibly tens of µm, unstated). And the dataset samples are **deuterated** (Pc-d₁₄:PTP-d₁₄), not the published protonated ones — their form and thickness may genuinely differ. Sharpest ambiguity: the published **0.1%** concentration exists only as a **100 nm evaporated thin film on glass**, while crystals are 0.01% — if the dataset's 0.1% sample were a film rather than a crystal plate, the geometry (and 3b) changes completely. **Per-sample form + thickness is therefore the top Angus ask.**
  - **Mann JACS SI read (2026-07-04) — second rig configuration on record; cw spot still unstated.** The Mann 2025 SI (§S1.B + Fig. S1) describes a configuration that differs from Mena 2024's: excitation **532 nm Nd:YAG (Laser Quantum "Finesse")** through a 520/40 bandpass — not the LP520 diode; a **single molded aspheric (Thorlabs C061TMD-A; f = 11.0 mm, NA 0.24, catalogue values checked 2026-07-04)** both focuses excitation and collimates PL for **cw and pulsed alike** (the paths split downstream: cw-ODMR/Rabi → multimode fibre → FEMTO OE-200-SI + SR830 lock-in; pulsed → single-mode fibre → APD); MW to **crystals via a 1-mm-ID planar loop antenna**, to thin films via PCB CPW. No spot size, beam diameter, M², or absolute power at sample appears in main or SI (only "lower laser power" for DAP, unquantified). Pulsed duty is printed (5 µs init + 1 µs read per 600 µs for Pc, ≈1%); cw-ODMR is genuinely cw. Same SI re-corroborates: 0.01% crystals polished to **0.5 mm plates**; the **only published 0.1% Pc:PTP remains a 100 nm OMBD film on glass** ("for maximal contrast, we use thin films" — the group's stated preference at readout-limited contrast); **no deuterated samples appear**; mounting/wax unstated (Mena's glass-slide + paraffin-wax remains the only published mounting). **Consequence:** do not assume the f = 30 mm doublet for the dataset's cw path — *which rig took the dataset* is now explicitly part of Angus ask 2 (2026-07-06 numbering; was ask 3); the dataset's drive-current axis (50–100 mA) is diode-like (Mena rig), unconfirmed.
  - **Consequence — analytic anchor:** the rig anchor becomes **Gaussian-spot-on-layered-medium spreading resistance (Hankel transform)** (§7.T1 geometry split); cylinder Bessel/Robin anchors remain for the maser-crystal geometry.
  - **Consequence — check 3a regime dependence:** if spot radius w ≪ flake thickness t, the near-field ΔT is dominated by intrinsic k_PTP spreading resistance → 3a genuinely calibrates the material and **transfers** to the maser geometry. If w ≳ t, substrate + interface conductance dominate → 3a calibrates *the rig*, not the material. **w/t is the single most load-bearing unknown for 3a.** (At the published values — w plausibly tens of µm, t = 0.5 mm — w ≪ t would hold comfortably; neither number is confirmed for the actual dataset samples.) **Hankel identifiability sweep ranges (set by the 2026-07-04 Mann-SI finding):** with two candidate optics on record and no spot stated for either, sweep spot radius w over ~**1 µm** (diffraction-limited f = 11 mm NA 0.24 aspheric, filled aperture) to ~**100 µm** (f = 30 mm doublet, tens-of-µm class with margin), and thickness t over {**100 nm** film, **0.5 mm** plate}. Every published-optic scenario keeps w ≪ t for crystal plates (3a transfers to the material); the film case inverts to w ≫ t regardless of optic (3a calibrates the rig/substrate stack, not k_PTP) — the per-sample-form ask decides which branch is live. **Sweep run 2026-07-04 — regime flag RESOLVED (full decision report: `thermal/reports/identifiability_3a.md`; engine `src/cavity/thermal/`, §8-discipline anchors green, verdicts regression-pinned in `tests/test_thermal_identifiability.py`).** Box actually swept: w = 1–500 µm (upper end extended past the ~100 µm above to cover an unfocused/misaligned doublet with margin), t_wax = 1–100 µm, k_PTP = 0.1–1.0 W m⁻¹ K⁻¹, both forms, σ_rel ∈ {5, 10, 20}%. Verdicts (reworded 2026-07-05 into factorisation terms — same numbers, same verdict labels): **PLATE = CONDITIONAL — conditional on independent w knowledge to ×3.** In the spreading regime the plate observable is the **product k·w** (ΔT ∝ 1/(k·w); ∂lnΔT/∂ln k = ∂lnΔT/∂ln w = −1.00 — the same variable, one appearance): the measurement never contained k alone, so no amount of data precision separates the factors; an independent w measurement is what *factorises* the product, and 3a's precision on k is set, multiplicatively, by the precision on w — required w-prior half-width ×2.3–3.2 (sweep values across σ_rel; cap √(k_hi/k_lo) = √10 ≈ 3.16 from the band width). Signal is never the issue (the k band swings the observable by R ≈ 2.6–2.8× the mid-band signal — identifiable-by-signal over 100 % of the nuisance box at every σ_rel); t_wax and t_glass are invisible (≤ 0.3 % / ≤ 1 %). **FILM = UNIDENTIFIABLE — insensitive, not underdetermined.** The film's k-lever is ∂lnΔT/∂ln k ∈ [−0.11, −0.004] — near zero: a film measurement measures the wax+glass stack with k_PTP as a rounding error, so better nuisance characterisation cannot recover k from a film — **the lever is absent, not hidden** (the sweep's 93–99 %-confounded-even-with-w-pinned figure is that absence expressed in the confounding metric; R > 2σ_rel only at w ≲ 12 / 5.5 / 2.2 µm). Corollary for 3b: mixed-form samples break its rig-cancellation (Θ_plate/Θ_film = 0.78–3.36 across the box) — ask 1 protects 3b as well as 3a. **Volumetric appendix run 2026-07-05 (`thermal/reports/identifiability_3a_volumetric.md`; engine anchors + pins in `tests/test_thermal_volumetric.py`) — verdict SOFTENS, in the argument's favour:** under buried (Beer-Lambert) deposition the pure k·w law generalises — the plate observable is **k × g(w, l_abs)**, a geometry factor with weakened w-dependence (w log-slope −1.0 → −0.2 across l_abs = 5–200 µm; §6T `PumpAbsorptionLength` scoping values, UNSOURCED-SCOPING) while the k log-slope holds at −1.00…−0.95. Two consequences: (i) the de-confound w-prior only **loosens** with burial (up to ×23.6 at the focused point) — the surface-limit ×2.3–3.2 is therefore the conservative floor, and the "rough rig ID suffices" argument stands, now with demonstrated margin rather than assumption; (ii) **l_abs is a second factorisation input**, needed to ×3.0 worst-case — recorded as a rider on §11 item-5 asks 1–3 plus the raw-data rider (2026-07-06 numbering; was asks 1/3/4/6) plus a literature pull on our side: prefer **measured** penetration data (Takeda 2002 / Breeze-era) over computing l_abs from nominal doping, because nominal vs actual pentacene incorporation in Bridgman growth differ substantially — **upgraded 2026-07-06 from prudent to supervisor-backed, with a direction (Oxborrow-verbal): the growth zone-refines, so actual [Pc] < nominal by an unknown factor, and nominal-doping l_abs overstates absorption / understates penetration**; the same nominal-vs-actual systematic touches 3b's concentration ratio (flagged, not re-analysed). The §11 item-5 rationale lines for asks 1 and 3 are aligned to this framing — the re-ranking decision the first pass deferred, taken 2026-07-05 after reading both reports; ask order unchanged. *(2026-07-06: list finalised at three and renumbered — form/rig/power are now asks 1/2/3, substrate demoted to a rider; relative order of the three preserved.)*
  - **Consequence — check 3b:** in a thin sample the absorbed fraction goes as **concentration × thickness** whenever the absorption length is not small vs t (in 0.01% material it may well exceed t at the pump wavelength — 520 nm on this rig, per Mena Methods). The concentration-ratio check therefore acquires exactly **one geometric input: per-sample thickness (and form)**. It remains laser-power-independent and remains **first to implement**. **Nominal-concentration caveat (Oxborrow-verbal, 2026-07-06):** Bridgman growth zone-refines, so BOTH samples' actual [Pc] sit below nominal by unknown incorporation factors — the nominal 10:1 ratio itself carries this systematic and 3b may not use nominal values without stating it. The ratio cancels the factor only if incorporation efficiency is common to both concentrations, which nothing in hand establishes (the meeting confirmed direction and mechanism, not concentration-independence) — state the assumption when 3b is run.
- **Model-boundary decision (recast from §6T/§7T's original scope).** The *calibration* model boundary is **crystal + mount in the ODMR rig** — the crystal→STO thermal-path extension (conduction through mount/air gap, radiation) is **not** needed to reproduce the Glasgow data. It **is** needed for the maser-geometry prediction (observable b) and for Phase 3's coefficients. Sequence accordingly: calibrate (a) first, then transfer the validated transport core to geometry (b). **ΔT numbers do not transfer between geometries; the transport model and dν/dT do.**
- **Regime note.** The Glasgow data is **steady-state CW** — exactly the regime of the collaboration's cw-cooperativity projections (Ziqiu Huang's work), and distinct from the pulsed-Xe Breeze/Booth transients this project also carries. Both regimes stay in scope; the calibration lands in CW.

**Validation checks, in order of what they test (supersedes "validated only internally"):**
1. **Internal / limit checks** — energy balance, closed-form limits (§8's discipline extended to `thermal/`). Necessary, not sufficient.
2. **Order-of-magnitude vs Wu** — the submodel's ΔT should be consistent with Wu's single lumped estimate to within the expected spatial-resolution correction.
3. **Primary, data now in hand at scraped-points level: reproduce Cowley-Semple.** Sub-checks:
   - **(3a) Slope magnitude** — predicted dν/dI for the 0.1% sample within the df_spin/dT spread band (§6T), given calibrated laser power + spot size. Tests **k and the BCs**. *Constraint honesty:* steady-state points constrain k and boundary coefficients; they do **not** constrain ρc_p (Chang carries c_p independently, §6T) — say so in the paper; a matched slope is not a validated transient model.
   - **(3b) Concentration scaling** — ratio of 0.1% to 0.01% sensitivities. Same setup, same currents ⇒ **laser-power calibration cancels**; tests the q̇ ∝ [Pc] absorption model nearly free of the largest metadata uncertainty. The strongest cheap check in the project — do not skip it.
   - **(3c) Timescale — conditional.** Only if time-resolved traces exist (§11, item 5, ask). If they do, they are the sole probe of α = k/ρc_p and promote the transient model from analytic-check-validated to data-validated.

**Claim levels (restated):** 3a+3b met → *"explains observed, previously unmodelled behaviour"* (strong). Without → checks 1–2 only, claim softens to internally-consistent-and-order-of-magnitude-correct. Timescale language is conditional on 3c — do not state "magnitude and timescale" as a blanket requirement.

**7.T6. Mitigation variants (UNRATIFIED BRAINSTORM — Oxborrow email 2026-07-04; off the critical path).** Two ideas from the reply, logged so they neither get lost nor silently promote themselves into scope:

- **Crystalline-sapphire multiblade waveguide doubling as heat sink** for the illuminated PTP (the waveguide is Vanessa's workstream). If it ever needs numbers, it is representable in the Phase-2 thermal model as **one boundary-condition variant** — a **finite-conductance Robin contact** on the sunk surface. **Framing corrected 2026-07-06 (Oxborrow-verbal): the sapphire is itself not a free cold boundary — it has to be cooled too.** The earlier "fixed-T contact face" option is retired as an idealisation that overstates the sink; if the variant is ever exercised, it is a finite-conductance contact, full stop. Still not modelled now. No coupling to Vanessa's ray-tracing required; no new geometry.
- **Fluid-carrying capillaries inside the maser crystal** (heat removal and/or pump-light delivery): **future-work prose only — never modelled here** (§9).

Neither enters the deliverable; neither has been ratified by anyone.

**7.T7. Radiation balance in the low-k regime (SENSITIVITY FLAG — Oxborrow-verbal, 2026-07-06; a check against the existing model, not a rebuild).** In poor thermal conductors — p-terphenyl is one; that is the project premise — thermal RADIATION becomes significant relative to conduction "sooner" than intuition trained on good conductors suggests. Run as a sensitivity check on the conduction-dominated model: quantify where the radiative boundary term stops being negligible vs the conductive term across the plausible operating range (surface T, geometry, an emissivity band), rather than assuming it small. First implementation slot: the standard linearised radiative coefficient h_rad = 4εσT³ (~6 W m⁻² K⁻¹ at 300 K, ε = 1) folds directly into the switchable Robin `h_top` of `thermal/layered.py`, so the leading-order check reuses the §7.T4-convection machinery; report where h_rad·ΔT competes with the conductive flux, and where the linearisation itself fails (ΔT no longer ≪ T — relevant given the "several tens of Celsius" inference, §11 item 5). Corroborating meeting points, recorded in the same place: (a) a more collimated beam drives light deeper and avoids an interface hot spot — independent (verbal) support for the volumetric Beer-Lambert deposition already appended (§7.T5 appendix), NOT surface-only; (b) the sapphire heatsink finite-conductance reframing above (§7.T6).

---

## 8. Analytic benchmark (traceability anchor — build and pass FIRST) — UNCHANGED

Validate the EM solver against closed-form modes before trusting it on the STO ring (Oxborrow-2007 traceable-FEM principle).

- **Empty right-circular copper cylinder**, radius a, length L: TM_mnp f = (c/2π)√[(x_mn/a)² + (pπ/L)²]; TE_mnp f = (c/2π)√[(x′_mn/a)² + (pπ/L)²]; **TE011** uses x′_01 = 3.8317. Compare to an empty-cavity COMSOL solve. **Target < 0.1%.** Pure-Python (scipy Bessel zeros) — CI-testable without COMSOL.
- **Q-from-tanδ closed checks:** filled cavity → Q_diel = 1/tanδ; with filling factor → Q = 1/(p_e·tanδ). Pins the Q convention (§3). *(Status 2026-07-06, live licence-session run: the §5 gate's live PEC+lossy arm clears the 0.5% tolerance at the gate's default mesh level — dielectric 1.25×10⁻⁴ m / air 5×10⁻⁴ m, the §2 e2e finest ladder level, assumed a/L = 0.5 puck — with residual 5.6×10⁻⁵, ~1% of tolerance consumed. A convention pass at that level, not a mesh-independence result — convergence remains §2's job. Full numbers single-sourced in `provenance/constants.py`, `ExtractionTolerances.q_pec_lossy_rel_tol`; artifact git-tracked at `refs/gate_runs/20260706T211615Z_live_comsol`.)*
- Citation: **Oxborrow 2007, IEEE Trans. MTT 55, 1209.**

*(The thermal submodel gets its own analytic limit checks — e.g. uniform source in a slab with fixed-T substrate has a closed-form parabolic profile; a point/line source has a known Bessel/Green's-function form; a Gaussian surface spot on a half-space / layered medium has a closed-form Hankel-transform spreading resistance — the rig-geometry anchor of §7.T1/§7.T5. The §7.T2 output-3 line observable adds its own set: uniform T ⇒ zero inhomogeneous width — exact; probe-weighted moment closed forms on the half-space (elliptic-integral second moment) and in the 1-D limit; and a Hankel-Parseval identity (probe-weighted mean = centre rise of a √2-wider spot) as independent algebra. Assert these in `thermal/` CI, same discipline as the EM benchmark.)*

---

## 9. Non-goals / out of scope

Unchanged EM/dynamics exclusions:
- **No 3D** (except a bounded eccentricity side-study if §7-expanded §7.4 requires it). Axisymmetric m=0 for the eigenmode; coupling loop/port out of scope.
- **No Maxwell–Bloch time evolution** — Niall/Nina's project (arXiv 2412.21166). This repo *feeds* it; does not reimplement.
- No spin dynamics / PyCCE.
- No assumption of a COMSOL licence in CI.

**Explicitly out of scope from the 2026-07-01 meeting (PI brainstorm → future-work pointers only, NOT this UROP):**
- **No resonator shape optimisation.** Swiss-cheese / starfish / high-surface-area bodies, prismatic / fettuccine / flat-planar geometries, high-aspect-ratio cross-sections, the "reactor-core" holed cylinder, "best shape for a flat 4mm×8mm target," the filling-factor×Q "grand challenge" — all a different, much larger research program. Note as future work in the eventual paper; do not attempt.
- **No cooling-channel / duct engineering.** Growing channels into the resonator, flowing air/liquid coolant — out of scope. This now explicitly covers the 2026-07-04 brainstorm items (§7.T6): **capillaries in the maser crystal** are future-work prose only, never modelled; the **sapphire heat sink** enters the model — if at all — only as §7.T6's single BC variant. Convection enters only as a BC-coefficient sensitivity (§7.T4).
- **No materials survey** for alternative dielectrics (better-than-STO search). The deliverable is on the *existing* STO/pentacene system.
- **No loop-gap / metallic re-entrant / flat-target resonator design.** Double-flagged: (i) that is **Zangwill's active project** by name — do not overlap; (ii) out of 8-week scope regardless. Oxborrow raised loop-gap-out-of-metal mid-meeting; the boundary needs stating to him in writing (§11) so it doesn't harden into an assumed scope.

The one part of the meeting's riff that *is* in scope is the analytical thermal submodel on the current geometry (§7T) — because it upgrades the existing pipeline without a geometry departure, needs no new licence, and plugs straight into the C₀ → thermal-budget chain.

---

## 10. Suggested module shape (agent finalises — not prescriptive)

Logical components: `forward_model` (build + solve, complex eigenvalue + field handles) · `extraction` (§3 integrals, Jacobian-correct; now also exposes κc and gain-region H) · `validation` (§5 gate + §8 benchmark + §5a checkpoint) · `sweep` (parametric driver over §7 DOFs) · `surrogate` (PCE/GP fit + predict + CV gate) · **`thermal`** (NEW — the §7T closed-form heat-conduction submodel, Bessel/Hankel, BC-honest, with its **two anchor geometries**: rig flake-on-layered-substrate for calibration, maser cylinder for prediction; its own analytic CI checks) · `mc_yield` (sample + aggregate + Sobol; SiPhON port) · `provenance` (§6 **and §6T** constants as one typed single-source object).

**`mc_yield` output-layer retarget (not an architecture change).** Sampling + surrogate machinery over the DOF space is untouched — same input space, same surrogated forward model, out come C₀ and κc. What changes is post-processing: instead of thresholding at C₀ > 1, compute Δf_max = (κc/2)√(C₀−1) per draw → ΔT_max / P_max via `thermal` → report *those* distributions. Small, contained. **Rename candidate:** "yield" no longer describes the output — consider `robustness/` or `thermal_margin/`. Flag; decide with supervisor.

Keep the §6/§6T numbers in exactly one place; everything imports them.

---

## 11. Open gaps to resolve (flagged, do not paper over)

Carried EM gaps:
1. **Booth's dielectric cross-section under-specified** (height / puck-vs-torus) → obtain Booth's `.mph`; until then `dielectric_shape` switch + height sweep, §4 as empirical closure.
2. **22-GHz STO tanδ primary source** (likely Geyer et al., JAP 97, 104111 (2005)) — pull for writeup.
3. **Wu coupling coefficient unstated** → de-loading assumes k ≈ 0.2.
4. **COMSOL complex-eigenfrequency sign/convention for Q — RESOLVED (supervisor confirmation, in person, 2026-07-02).** (a) The STO model is built in the **packaged RF interface** (`Electromagnetic Waves, Frequency Domain`, tag `emw`) — not a custom weak-form PDE; (b) the Q convention is **Q = f′/(2 f″)** from the complex eigenfrequency, exactly as §3 already assumes. The `refs/comsol/booth/2D Resonator Lossy.mph` reference uses f′/(2 f″) — confirmed directly by the supervisor, who pointed to the frequencies and had the Q computed from them in-session. The `refs/comsol/oxborrow/sapphire_ring_1p45_mag_GHz.mph` is **method-reference only** (weak form, lossless, no Q) — never a convention source. An earlier probe reading of `imag(emw.freq) = 0` on the lossy file was a **script bug** (indexing/eval-path), not a genuine energy-method convention — retracted; do not re-introduce. **Mechanism pinned (2026-07-03, empirically on the lossy file):** in results evaluation COMSOL realifies the interface-scoped `emw.freq` per solution number (its imag reads 0 even on a lossy solve); the complex eigenfrequency lives in the **bare solver variable `freq` = iλ/(2π)** (λ = δ − iω, so f′ = −Im(λ)/2π, f″ = Re(λ)/2π), and f′/(2 f″) from bare `freq` reproduces `emw.Qfactor` exactly, mode by mode. Code must read `real(freq)` / `imag(freq)`, never `imag(emw.freq)` — guarded in tests/test_comsol_mph_server.py. The §8 assertion (PEC + lossy dielectric ⇒ Q = 1/(p_e·tanδ)) stays in force as the permanent regression guard on the convention, not as an open question.

New (thermal pivot):
5. **Cowley-Semple ODMR dataset — RESOLVED (identity + existence), metadata pending.** What exists (from the 2026-06-26 "linewidths" thread, Glasgow/MIT/Imperial):
   - **Primary series (0.1% Pc-d₁₄:PTP-d₁₄):** CW-ODMR X–Z resonance vs laser drive current, same setup throughout: **1449.7 MHz @ 50 mA · 1449.4 @ 60 · 1449.1 @ 80 · 1448.6 @ 100** → LSQ slope **−21 kHz/mA**. Points scraped from thread figures — obtain raw.
   - **Control series (0.01% Pc-d₁₄:PTP-d₁₄):** same setup, visibly less sensitive (peak near-stationary 60–100 mA). Treat as an upper bound on its dν/dI until raw data lands.
   - **Linewidth table (feeds C-variance, not the thermal calibration):** 1.75 MHz (0.1% Pc:PTP) · 1.4 MHz (0.1% d₁₄) · **0.55 MHz (0.01% d₁₄)** · 7 MHz (picene) · 1.8 MHz (NAP). Best-per-host at differing MW/laser powers — not a controlled comparison; note it.
   - **ΔT inference (Oxborrow's, in-thread):** with dν/dT ≈ −0.05 MHz/K, the −21 kHz/mA slope ⇒ ~0.4 K/mA ⇒ **~13–30 K steady-state heating at 100 mA** across the coefficient spread (§6T) and plausible diode thresholds. "Several tens of Celsius."
   **Routing update (2026-07-04): asks re-route to Angus directly.** Oxborrow makes the introduction **Monday 2026-07-06 — he asked to be reminded that morning; send the reminder.**
   **Ask list FINALISED at three (Oxborrow-verbal, in-person meeting 2026-07-06) — lead with exactly these, in this order; everything else folds in behind as riders.** The rationale lines from the 2026-07-04/05 passes are retained below — confirmed this pass, they still hold. (Published-record partial answers in parentheses.)
   1. **Per-sample form + flake/plate thickness + lateral size — 0.1% AND 0.01% d₁₄.** Form decides whether 3a **exists**: a film has no k-lever at all (∂lnΔT/∂ln k ≈ 0 — insensitive, not underdetermined; §7.T5 sweep), so 3a is live only for a plate — ask first. Also decides 3b's geometric input and the w/t regime. (Published protonated record: crystals = 0.5-mm polished plates; the only published 0.1% sample is a 100 nm evaporated *film* — which are yours? *Mann SI checked 2026-07-04: pattern re-confirmed — 0.01% crystals = 0.5 mm plates, 0.1% only as 100 nm film, films preferred "for maximal contrast"; d₁₄ samples still absent from print. Ask stands, sharpened.*)
   2. **Which rig, and the cw spot size at sample** (objective NA or measured spot). (*Two configurations now on record — Mann SI 2026-07-04:* Mena 2024 cw path = LP520-SF15A diode + f = 30 mm doublet AC254-030-AB; Mann 2025 = 532 nm Finesse + C061TMD-A f = 11 mm NA 0.24 aspheric for cw *and* pulsed. Spot size stated in **neither**; your 50–100 mA drive-current axis looks diode-like — confirm which rig and the spot. NAP sample used C240TMD-B, f = 8 mm — different spot, do not pool.) Conditional on ask 1: **if plate**, the observable is the product k·w and an independent w is what factorises k out of it — decisive, to ×3 multiplicative (§7.T5 sweep); **if film**, moot (no k-lever). w also sets the w/t regime. *(Was ask 3 in the 2026-07-04 ordering; promoted 2026-07-06.)*
   3. **Laser power at sample and duty cycle:** wavelength (rig-dependent — 520 nm diode per Mena vs 532 nm Finesse per Mann; fold into ask 2); threshold current; slope efficiency (W/A) or measured power at each current. (*Mann SI 2026-07-04: no absolute power in print for either rig; cw duty = cw confirmed.*) *Without this, absolute ΔT(P) is uncalibrated; the ν(I) slope and the concentration ratio still are.* *(Was ask 4.)*
   Riders (fold in behind the three — already recorded, substance unchanged; old 2026-07-04 numbering in parentheses):
   - **Substrate material and mounting/adhesive** *(was ask 2)*. (Published: glass slide + paraffin wax — same for these samples? *Mann SI 2026-07-04: mounting unstated there; Mena remains the only published mounting.*)
   - **Any time-resolved traces** (shift vs time after laser on/off) *(was ask 5)*. Existence unknown — not in either published paper; decides whether §7.T5 check 3c is live.
   - **Raw ν(I)** for both concentrations (not scraped points), + uncertainties; raw spectra *(was ask 6)*.
   - **Co-authorship / acknowledgement expectations** for use of the dataset *(was ask 7)*.
   - *l_abs rider (2026-07-05, volumetric appendix; nominal-vs-actual clause upgraded 2026-07-06): the plate branch acquires a second factorisation input, pump penetration depth l_abs — needed only to ×3.0 worst-case. No new ask: covered by the metadata already in asks 1–3 plus the raw-data rider, plus a literature pull on our side — prefer measured penetration data (Takeda 2002 / Breeze-era) over computing l_abs from nominal doping, because nominal vs actual pentacene incorporation in Bridgman growth differ substantially — **Oxborrow-confirmed with direction (verbal, 2026-07-06): zone-refining ⇒ actual [Pc] < nominal, so nominal-doping l_abs understates penetration**; the same nominal-vs-actual systematic touches 3b's concentration ratio (§7.T5 3b caveat).*
   Sets (i) Phase 2's claim level (§7.T5) and (ii) the calibration model boundary — which is now **crystal + mount in the Glasgow ODMR rig**, not crystal→STO (§7.T5).
6. **Real pump illumination / power model** (replace Wu's numbers). Wu's ΔT ~ 0.3 K is from a *different pump setup*; the incident-intensity → absorbed-power → heat-source chain must be grounded in *this* device's pump, not inherited. — ACTION, ask supervisor.
7. **p-terphenyl thermal properties and coefficients — largely sourced; ρ still open.** k (floor), c_p, and df_spin/dT have primaries in §6T. **Nat. Commun. pull DONE (2026-07-04):** Singh et al. (Ajoy group), Nat. Commun. 16, 10530 (2025) — X–Z region-III slope **−101 kHz/K** (Fig. 2B(iii)); headline 247 kHz/K excluded (T_xy at the phase transition). **SI pass DONE (same day):** the SI also prints the slope bare (Table S1) — the hoped-for error bar definitively does not exist in print; raw data = Zenodo record **10.5281/zenodo.17231876, files restricted** (request access if the ± becomes load-bearing). In its place, Fig. 2B(iii) vector reanalysis (archived: `refs/singh_fig2biii_reanalysis.md` + `.csv`): drawn red line = **−112 kHz/K** over cold-finger 254–324 K; window systematics −68…−112; cold-finger axis with ≈ **+55 K** laser-heating offset at 110 mW cw; offset-corrected RT-local ≈ −70…−80 kHz/K. "RT-valid −101" downgraded accordingly — full grading in §6T; constant single-sourced in `provenance/constants.py` (with the new deuteration-transfer caveat). **ρ remains the one open pull.**
8. **Pre-Phase-2 C₀/κc re-run** (§5a) — confirm the thin-margin story on *your* model, not Breeze/Wu numbers, before building §7T on it.
9. **Q-vs-margin derivation rigor** (§7.T4) — derive C₀/κc joint DOF dependence from Layer A and confirm 1/√Q holds in ΔT space, before making it a headline claim. **Rung update (Oxborrow-verbal, 2026-07-06): the question is endorsed as worth deriving — pursue it.** The endorsement covers the question, not the result; the derivation requirement above is unchanged, and it now shares machinery and the linewidth reporting unit with the §7.T2 output-3 line observable (see §7.T4 linkage).
10. **Framing confirmation from Oxborrow.** He blessed thermal-modelling-as-direction and the analytical method; he has *not* explicitly ratified the budget-*distribution* framing, and the Q-margin *result* remains unratified — though as of 2026-07-06 (verbal) the Q-margin *question* is endorsed-to-pursue (§7.T4, item 9; originally he engaged, then went to shape optimisation). His in-thread coefficient inversion (revision-note endorsement ladder, above) is mechanism engagement on record — it is **not** ratification of the calibration-target plan; keep the two distinct in any written summary to him. Send the specific written proposal; treat this plan as pending his reply. Fold the §9 scope boundary (loop-gap = Zangwill, shape-opt = future work) into the same note so it doesn't drift into assumed scope.
    - **Nobody-else check — RESOLVED (Oxborrow email, 2026-07-04):** *"Neither Angus Cowley-Semple nor Ziqiu Huang is modelling thermal response."* Green field confirmed at supervisor level (ladder rung in the revision notes; corroborated by Mann 2025's author contributions, which put Ziqiu Huang on sample prep/characterisation). The Angus introduction is committed for **Monday 2026-07-06 — reminder required Monday morning** (item 5 routing). The parent bullet's framing items (budget-distribution, Q-margin) remain unratified — the 2026-07-04 reply does not touch them.
11. **SPEC §7 (expanded) revision** — its static-yield output framing is superseded (§7 here). The statistics (surrogate, CV gate, error budget, Sobol, tolerance curves) transfer; the output section and a thermal sub-section need the companion rewrite.
